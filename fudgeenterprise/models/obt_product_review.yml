version: 2
models:
  - name: obt_product_review
    description: The product review fact table combined with all of its outrigger dimensions into one bit table. Transaction fact table with one row per item ordered. Includes product data.
